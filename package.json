{
  "name": "drive-insight",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test:api:integration:auth": "dotenv -e .env -- jest --config apps/api/test/jest-integration-auth.json",
    "db:reset": "npx supabase db reset"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.2.0"
  }
}
